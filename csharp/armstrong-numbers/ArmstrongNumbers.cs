using System;

public static class ArmstrongNumbers
{
    public static bool IsArmstrongNumber(int number)
    {
        string digits = number.ToString().ToArray();
        Console.WriteLine("--", number)
    }
}